# Nginx 配置文件 - myworkbench-fe
# 使用方式：将此内容复制到 nginx 服务器的配置中

# zc.hqdx.store
server {
    listen 80;
    server_name zc.hqdx.store;

    # 前端静态资源目录
    root /opt/publish;
    index index.html;

    # 静态资源目录（OSS 挂载点）
    location /resources/ {
        alias /opt/resources/;
        autoindex off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 组件市场 manifest.json - 不缓存，确保获取最新版本
    location ~* /market/[^/]+/manifest\.json$ {
        alias /opt/market/;
        autoindex off;
        add_header Cache-Control "no-store, no-cache, must-revalidate, proxy-revalidate, max-age=0";
        add_header Pragma "no-cache";
        add_header Expires "0";
        add_header Access-Control-Allow-Origin *;
    }

    # 组件市场静态文件（除 manifest.json 外）
    location /market/ {
        alias /opt/market/;
        autoindex off;
        add_header Access-Control-Allow-Origin *;
    }

    # API 代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA 路由支持：所有非静态文件请求回退到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;
}

# m.hqdx.store
server {
    listen 80;
    server_name m.hqdx.store;

    # 前端静态资源目录
    root /opt/publish;
    index index.html;

    # 静态资源目录（OSS 挂载点）
    location /resources/ {
        alias /opt/resources/;
        autoindex off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;
}

# preview.hqdx.store
server {
    listen 80;
    server_name preview.hqdx.store;

    # 前端静态资源目录
    root /opt/publish;
    index index.html;

    # 静态资源目录（OSS 挂载点）
    location /resources/ {
        alias /opt/resources/;
        autoindex off;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 代理到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # SPA 路由支持
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # gzip 压缩
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;
    gzip_min_length 1000;
}
